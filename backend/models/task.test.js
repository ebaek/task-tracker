// const schema = require('./task.model');

// describe('Task model', () => {
//     test('Task should match the following schema', () => {

//         const properties = {
//             name: 'Vacuum the carpet',
//             description: 'Empty the dust bin',
//             dueDate: new Date(),
//             complete: False
//         };

//         for (let property in properties) {
//             expect(Task.schema.obj).toHaveProperty(property);
//         }
//     });
// });